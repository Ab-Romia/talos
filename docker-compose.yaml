services:
  postgres:
    image: postgres:latest
    hostname: postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
  adminer:
    image: adminer
    hostname: adminer
    restart: always
    ports:
      - "8080:8080"